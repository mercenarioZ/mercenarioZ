name: quote

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm i
      - name: Generate quote
        run: npm run gen
      - name: Update README.md
        run: |
          git config --global user.email "funnystar320@gmail.com"
          git config --global user.name "mercenarioZ"
          git add .
          git commit -m "Updated README.md" || echo "No changes"
          git push
